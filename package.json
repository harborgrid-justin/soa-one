{
  "name": "soa-one",
  "version": "1.0.0",
  "private": true,
  "description": "SOA One — Enterprise Business Rules Platform",
  "workspaces": [
    "packages/engine",
    "packages/server",
    "packages/client"
  ],
  "scripts": {
    "dev": "concurrently -n engine,server,client -c blue,green,magenta \"npm run dev -w @soa-one/engine\" \"npm run dev -w @soa-one/server\" \"npm run dev -w @soa-one/client\"",
    "build": "npm run build -w @soa-one/engine && npm run build -w @soa-one/server && npm run build -w @soa-one/client",
    "build:engine": "npm run build -w @soa-one/engine",
    "build:server": "npm run build -w @soa-one/server",
    "build:client": "npm run build -w @soa-one/client",
    "db:push": "npm run db:push -w @soa-one/server",
    "db:seed": "npm run db:seed -w @soa-one/server",
    "start": "npm run start -w @soa-one/server",
    "clean": "rm -rf packages/*/dist packages/*/node_modules node_modules"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.3.3"
  }
}
