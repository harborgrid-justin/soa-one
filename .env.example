# SOA One Configuration
DATABASE_URL="file:./soa-one.db"
PORT=4000
NODE_ENV=development

# Redis (optional - for production message queue)
REDIS_URL=redis://localhost:6379

# GraphQL
GRAPHQL_PATH=/graphql

# Rule Execution API
API_PREFIX=/api/v1
